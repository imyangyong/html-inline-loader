language: node_js

if: tag IS present

node_js:
  - 11.10.1

install:
  - npm install

deploy:
  provider: npm
  edge: true
  email: $NPM_EMAIL
  api_key: $NPM_TOKEN
  on:
    tags: true
    repo: html-inline-loader
  skip_cleanup: true

after_deploy:
  - echo npm published!😊

after_success:
  - cat ./coverage/lcov.info | ./node_modules/.bin/coveralls
